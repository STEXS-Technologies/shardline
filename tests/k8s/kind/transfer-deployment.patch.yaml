apiVersion: apps/v1
kind: Deployment
metadata:
  name: shardline-transfer
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: shardline
          env:
            - name: SHARDLINE_DEPLOYMENT_MODE
              value: insecure
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
