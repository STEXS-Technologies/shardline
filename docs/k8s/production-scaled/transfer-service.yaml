apiVersion: v1
kind: Service
metadata:
  name: shardline-transfer
spec:
  selector:
    app.kubernetes.io/name: shardline
    app.kubernetes.io/component: transfer
  ports:
    - name: http
      port: 8080
      targetPort: http
