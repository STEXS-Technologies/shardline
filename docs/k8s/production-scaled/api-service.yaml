apiVersion: v1
kind: Service
metadata:
  name: shardline-api
spec:
  selector:
    app.kubernetes.io/name: shardline
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
