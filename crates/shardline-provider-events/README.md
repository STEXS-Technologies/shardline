# shardline-provider-events

Provider webhook event processing, repository state tracking, and reconciliation
records. Defines event types and state machines for provider webhook delivery
(GitHub, GitLab, Gitea, Codeberg), tracks delivery outcomes, and manages
repository reconciliation state. Used by the server's provider integration layer.

See the [main Shardline README](../../README.md) for the project overview.
