//! Test-support modules for the `sdx` crate.
//!
//! [`hf_mock`] provides a HF-style Xet wire-protocol mock frontend used by the
//! M7 cross-frontend conformance suite (`tests/conformance.rs`).

pub mod hf_mock;
